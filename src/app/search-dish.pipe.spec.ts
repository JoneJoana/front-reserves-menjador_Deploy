import { SearchDishPipe } from './search-dish.pipe';

describe('SearchDishPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDishPipe();
    expect(pipe).toBeTruthy();
  });
});
